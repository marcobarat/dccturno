<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:u="sap.ui.unified">
    <u:Menu items="{path: '/menu'}">
        <u:MenuItem
            text="{nome}"
            startsSection="{nuovasezione}"
            select="{metodo}"
            enabled="{attivo}"
        />
       
    </u:Menu>

</core:FragmentDefinition>